
	------clusteringTests-----
	This program uses a euclidean clustering algarthem.
	After clustring each cluster is than represented as a sphere.
	each cluster retains it's location, approximant size, and approximant points.
	The name of the file to be processed is asked for at runtime.
	The name of the file to be saved is asked for at runtime.
	----variables----
	(int) maximum points
	(int) minimum points
	(float) cluster tolerance
