
	----ConvexHull----
	This program creats a canopy for the input cloud, uses an aglorthum to fill it's gaps and than does a quick triangulation from the point cloud library to create a mesh.

	----variables ----
	(int) Canopy Ratio
	(float) Search Radious 
	(float) mu
	(int) maxNeighbor
	(float) maxSurfaceAngle
	(float) minAngle,
	(float)maxAngle
